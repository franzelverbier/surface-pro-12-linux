# Remplace les deux "sleep 1" en aveugle par une attente de condition.
# ExecStart vide = on efface celui de l'unite d'origine avant de le redefinir.
[Service]
ExecStart=
ExecStart=/usr/local/bin/sp12-typecover-load
