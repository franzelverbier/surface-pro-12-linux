[Unit]
# Ordonne APRES la cible qui le veut : la cible n'attend donc plus sa fin.
After=multi-user.target
# Inutile et nuisible en EL2 : le script fait un modprobe explicite qui
# contourne la liste noire et relance 77 s de tentatives dans le vide.
ConditionKernelCommandLine=!modprobe.blacklist=ath12k_wifi7
