# Neutralise /usr/lib/modules-load.d/cdrecord.conf, qui demande le module SCSI
# generique "sg". CONFIG_CHR_DEV_SG n'est pas construit et cette machine n'a pas
# de lecteur optique : la demande echouait a chaque demarrage.
# Fichier volontairement vide (hors commentaires) : une surcharge dans /etc
# masque celle de /usr/lib.
